import RootHead from "../../../components/RootHead";

export default function Head() {
  return (
    <RootHead>
      <title>Sign in to Slash</title>
      <meta name="description" content="Sign in to Slash - slash.ly" />
    </RootHead>
  );
}
